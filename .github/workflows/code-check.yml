name: code-quality

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]
  workflow_dispatch:
  workflow_call:

permissions:
  contents: read

concurrency:
  group: code-quality-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up uv and Python
        uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          version: "0.12.8"
          python-version: "3.14"
          activate-environment: true
          enable-cache: true

      - name: Install quality tools
        run: uv pip install --editable ".[dev]"

      - name: Check formatting
        run: black --check datamol tests .github/scripts

      - name: Lint
        run: ruff check datamol tests .github/scripts

      - name: Test release validation
        run: python -m unittest discover -s .github/scripts -p 'test_*.py'

      - name: Build distributions
        run: uv build --no-sources

      - name: Validate distributions
        run: uvx twine check dist/*

      - name: Smoke-test wheel
        run: uv run --isolated --no-project --with dist/*.whl python -I .github/scripts/smoke_test.py

      - name: Smoke-test source distribution
        run: uv run --isolated --no-project --with dist/*.tar.gz python -I .github/scripts/smoke_test.py
